# ==============================================================================
# PostgreSQL to Databricks AI Migration Factory - Environment Configuration
# ==============================================================================
# You can copy this file to '.env' in your project root directory.

# ------------------------------------------------------------------------------
# 1. APPLICATION & AUTHENTICATION
# ------------------------------------------------------------------------------
ENVIRONMENT=DEV
JWT_SECRET=replace-with-at-least-32-random-characters-for-jwt-signing
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Optional bootstrap admin credentials (default created if omitted: admin / Admin@123456)
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=Admin@123456

# ------------------------------------------------------------------------------
# 2. SOURCE DATABASE: POSTGRESQL (Active Default)
# ------------------------------------------------------------------------------
SOURCE_TYPE=POSTGRESQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=your_postgres_db
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_SCHEMA=public
POSTGRES_SSLMODE=prefer

# ------------------------------------------------------------------------------
# 3. SOURCE DATABASE: SQL SERVER (Optional / Fallback)
# ------------------------------------------------------------------------------
SQLSERVER_HOST=localhost
SQLSERVER_DATABASE=
SQLSERVER_DRIVER=ODBC Driver 18 for SQL Server
SQLSERVER_USERNAME=
SQLSERVER_PASSWORD=

# ------------------------------------------------------------------------------
# 4. TARGET: DATABRICKS & UNITY CATALOG
# ------------------------------------------------------------------------------
DATABRICKS_HOST=https://<your-workspace-instance-id>.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/<your-sql-warehouse-id>
DATABRICKS_TOKEN=dapi_your_databricks_personal_access_token

# Unity Catalog targets
DEV_CATALOG=migration_dev
TEST_CATALOG=migration_test
UAT_CATALOG=migration_uat
PROD_CATALOG=migration_prod
CONTROL_CATALOG=migration_control
CONTROL_SCHEMA=migration_control

# ------------------------------------------------------------------------------
# 5. GOVERNED AI REMEDIATION (Ollama / Gemini / OpenAI)
# ------------------------------------------------------------------------------
# Option A: Local Ollama (Recommended for development, no API key needed)
LLM_ENABLED=false
LLM_PROVIDER=OLLAMA
LLM_BASE_URL=http://127.0.0.1:11434
LLM_API_KEY=
LLM_MODEL=qwen2.5-coder:3b
LLM_TIMEOUT_SECONDS=120
LLM_MAX_ATTEMPTS=3
LLM_NUM_CTX=8192
LLM_NUM_PREDICT=4096
LLM_MAX_PROMPT_CHARS=160000
OLLAMA_KEEP_ALIVE=5m

# Option B: Google Gemini (Alternative cloud provider)
# LLM_ENABLED=true
# LLM_PROVIDER=GEMINI
# LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta
# LLM_API_KEY=<your-gemini-api-key>
# LLM_MODEL=gemini-3.6-flash

# ------------------------------------------------------------------------------
# 6. MIGRATION EXECUTION & PERFORMANCE POLICIES
# ------------------------------------------------------------------------------
DEPLOYMENT_POLICY=FAIL_ON_SCHEMA_DRIFT
DEV_SCHEMA_POLICY=REPLACE_CHANGED
BATCH_SIZE=10000
PARALLELISM=4
LOAD_MODE=FULL_LOAD

# Optional row cap for testing (leave commented out for full table migration)
# MAX_ROWS=1000
